# react-day-picker@8 and react-popper@2 declare peer support only up to React 18,
# while this project runs React 19. The installed tree works, so relax npm's peer
# resolution instead of letting it try to downgrade React.
legacy-peer-deps=true
